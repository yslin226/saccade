"""Visual actions — the ways the agent can change what it is looking at.

These are the "saccade" half of the library: crop, zoom, annotate. They are
public so callers can add their own.

Implementations land in M1; M0 only reserves the package.
"""

from __future__ import annotations

__all__: list[str] = []
