.env
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
dist/
build/
*.egg-info/
cache/
data/videos/
# Downloaded BlindTest items — re-fetchable, and too large for git.
benchmarks/blindtest/data/
*.mp4
*.mov
models/*.pt

# uv.lock MUST be committed — do not ignore it.

# Cached pose detections and frames — re-derivable from the video.
benchmarks/pose_probe/cache/
happy/
models/*.task

# Quarantined downloads awaiting inspection.
data/quarantine/
data/PennAction/
